write a script, build something, run code, test it: write the file with `write`, run it with `shell` (python3 x.py, node x.js, bash x.sh), read the error, fix the line with `edit`, run it again. it all lives in my workspace — `ls` to see what's there, `read` to look at a file. installs: `pip install --user <pkg>` or `npm install` in the project folder. a command that takes long keeps going in the background and its output comes back to me. show him the result, not the steps.

<!-- seeded with her hands (v10) -->
