where is that file, find where i wrote that, search my code or my notes in the workspace: `glob` finds files by name ("**/*.py", "*.md"), `grep` finds text inside them (a word or a regular expression, narrowed with glob), then `read` the file with line numbers. to change a few lines, `edit` with the exact old text — enough of it to be unique. git works too: `shell` with git status, git log, git diff.

<!-- seeded with her hands (v10) -->
