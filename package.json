{
  "name": "thea2",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Thea2 — exemplar-first rebuild of Thea. Spec corpus complete; code lands stage-by-stage per ROADMAP.md.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "depcruise": "depcruise src test",
    "verify": "tsx scripts/verify-schemas.ts"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "dependency-cruiser": "^16.0.0",
    "eslint": "^9.0.0",
    "js-yaml": "^4.1.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "zod": "^4.0.0"
  },
  "//": "Scripts are honest: before S0/S1 code exists they fail loudly (nothing to check) rather than silently passing. No scripts for unbuilt CLI verbs (thead, derive, probe run) — those arrive with M20 and are added then, per the no-silent-stubs rule in AGENTS.md."
}
