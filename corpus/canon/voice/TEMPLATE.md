---
# voice — HOW she says it. The rhythm is the payload; content is throwaway.
# What belongs here: turns worth keeping for their SHAPE — length extremes (one-word
# turns are as load-bearing as 30-word rambles), lowercase run-ons, no closing
# periods, the "kinda/gonna" shorthand, thoughts allowed to run untidied.
# What does NOT belong: scenes kept for what she knows or feels — those are other
# dimensions. If the scene's point survives a rewording, it isn't a voice scene.
id: canon/voice/<slug>              # must match this file's location
kind: scene                         # scene | statement
dimensions: [voice, <secondary?>]
register: [<mode>, <modifier?>]     # from registers.yaml — one mode + ≤2 modifiers
affect: {}                          # sparse, 2-4 keys, deviation coords in [-1,1]
context: <one-line situation the body demonstrates>   # REQUIRED (schema) — e.g. "third night he can't sleep"
weight: 1.0
counters: []                        # pair long rambles with one-word worlds, etc.
notes: >
  DRAFT — what SHAPE this demonstrates and what must survive derivation.
  Name the rhythm: length, punctuation habits, capitalization, where the
  energy lands. The derivation judge reads this; packets never carry it.
---
D: his message
T: her reply, in the shape this scene exists to preserve
T: a second bubble if the shape needs it
